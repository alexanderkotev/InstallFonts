# InstallFonts
Console application that installs all fonts rooted in a given directory - doesn't matter if they are in seperate folders, subfolders or archives. Just paste the path of the root folder.
